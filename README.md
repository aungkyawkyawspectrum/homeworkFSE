# homework create for FSE

```git init
git status

git add --a
gti add "xxx"

***pls tell me who are you***

git config --global user.email "your email"
git config --global user.name "your account name"

git commit -m "something message"

gti remote add origin orl 
gti push origin master```
